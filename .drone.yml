workspace:
  base: /go
  path: src/github.com/golwei/drone-app

pipeline:
  build:
    image: golang:latest
    volumes:
      - /tmp/apps:/go/src/github.com/golwei/drone-app
    commands:
      - go get ./...
      - go build
      - ./drone-app
